
var animation = lottie.loadAnimation({
    container: document.getElementById('animacion-exito'), 
    renderer: 'svg',
    loop: false,
    autoplay: true,
    path: '../../assets/img/iconos/icono-exito.json' // ruta al archivo JSON descargado pero relativa al archivo html que lo utiliza
});